import React from 'react';
import './Scan.css'; // Import the CSS file

const Scan = () => {
  return (
    <div className="scan-container">
      <img src="./scan.png" className="full-width-image" />
    </div>
  );
};

export default Scan;
